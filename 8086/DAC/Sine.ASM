	OUTPUT 2500AD
.ORG 2000H

DATA SEGMENT
LIST DB 80H, 98H, B6H, D0H, E8H, F5H, FCH, FFH
     DB FCH, F5H, E8H, D0H, B6H, 98H, 80H, 68H
     DB 50H, 38H, 20H, 10H, 08H, 03H, 00H, 03H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

START:
    MOV AX, DATA
    MOV DS, AX

    MOV AL, 80H
    MOV DX, FFE6H
    OUT DX, AL

L1:
    MOV SI, OFFSET LIST
    MOV CX, 18H

L2:
    MOV AL, [SI]
    MOV DX, FFE0H
    OUT DX, AL
    INC SI
    CALL DELAY
    LOOP L2

    JMP L1

DELAY:
    MOV BX, 5555H
L3:
    DEC BX
    JNZ L3
    RET

CODE ENDS
END